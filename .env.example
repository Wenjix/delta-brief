# OpenAI API Key (for LLM generation)
VITE_OPENAI_API_KEY=your_openai_api_key_here

# Memory Provider Configuration
# Options: 'local' (localStorage) or 'memmachine' (MemMachine API)
VITE_MEMORY_PROVIDER=local

# MemMachine Configuration (only used when VITE_MEMORY_PROVIDER=memmachine)
VITE_MEMMACHINE_BASE_URL=/memmachine
VITE_MEMMACHINE_ORG_ID=patched
VITE_MEMMACHINE_PROJECT_ID=delta-brief-demo
